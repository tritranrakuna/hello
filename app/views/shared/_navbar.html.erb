<nav class="navbar is-link" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a class="navbar-item"> 
        <%= link_to "TWEETER", root_path, class: "navbar-item" %>
    </a>

    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          
            <% if user_signed_in? %>
              <p> <%= link_to current_user.email.split('@')[0], edit_user_registration_path, class: "button is-info"%> </p>
              <p> <%= link_to "Logout",  destroy_user_session_path, method: :delete, class:"button is-info" %>
            <% else %>
              <p> <%= link_to "Sign Up",new_user_registration_path, class:"button is-info" %>
              <p> <%= link_to "Sign in",new_user_session_path, class:"button is-info" %>
            <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>